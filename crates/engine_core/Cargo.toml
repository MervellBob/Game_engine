[package]
name = "engine_core"
version = "0.1.0"
edition = "2024"

[features]
override = []

[dependencies]
events = { path = "../events" }
rendering = {path = "../rendering"}
tracing = "0.1.44"
gl = "0.14.0"

